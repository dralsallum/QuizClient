import { PLanguageImgCanvas } from "./components/Project/Project.elements";

const AvatarComponent = ({ imgPath }) => {
  return <PLanguageImgCanvas src={imgPath} alt="Avatar" />;
};

export default AvatarComponent;
